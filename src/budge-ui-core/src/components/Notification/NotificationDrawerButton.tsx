import React from "react";
import { TNotificationDrawerProps } from "./types";

const NotificationDrawerButton = (props: TNotificationDrawerProps) => {
  return null;
};

export default NotificationDrawerButton;
